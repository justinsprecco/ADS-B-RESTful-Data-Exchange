{
  "name": "ads-b-restful-data-exchange",
  "version": "1.0.0",
  "description": "Sagetech Capstone Project",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix brokerService\" \"npm run dev --prefix usersService\" \"npm run dev --prefix oauthService\"",
    "lint": "eslint . --fix",
    "test": "jest",
    "install-all": "concurrently \"npm install --prefix brokerService\" \"npm install --prefix usersService\" \"npm install --prefix oauthService\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@stylistic/eslint-plugin-js": "^2.10.0",
    "concurrently": "^9.1.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.7"
  }
}
